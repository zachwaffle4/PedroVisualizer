[package]
name = "pedro-visualizer"
version = "0.0.0"
description = "Pedro Pathing Visualizer"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "pedro_visualizer_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
